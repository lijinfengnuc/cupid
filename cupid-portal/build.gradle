import org.apache.tools.ant.filters.ReplaceTokens

plugins{
    id 'org.springframework.boot'
}

apply plugin: 'io.spring.dependency-management'

description = 'Front api project'

dependencies {
    api project(':cupid-arrow')
    api project(':cupid-mall')
    api project(':cupid-account')
}

processResources {
    exclude('conf')
    from('src/main/resources/conf') {
        include("application-${rootProject.ext.profile}.yml")
    }
    from('src/main/resources/application.yml') {
        filter(ReplaceTokens, tokens: [profile: rootProject.ext.profile])
    }
}

bootJar {
    baseName('cupid-portal')
}
